includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6
    # tests/ is out: Pest binds $this inside test closures at runtime, which PHPStan cannot see.
    paths:
        - app/
        - bootstrap/app.php
        - config/
        - database/
        - routes/
